---
name: shop-report
description: 편집샵 주간·월간 매출 리포트. 기간 매출을 집계해 채널별 매출·마진율·베스트셀러·재고 경고를 Discord로 보고한다. Trigger — "/shop-report", "매출 리포트", "이번 주 매출", "이번 달 정산", "주간 리포트", "월간 리포트". (comad의 reports-registry 스킬 /report 와는 별개)
---

# /shop-report — 매출 리포트

데이터 조회: `python3 ~/.claude/skills/sales/scripts/notion_sales.py sales-range <from> <to>`
(sales 스킬의 헬퍼를 공유한다. 별도 스크립트 없음 — 집계는 Claude가 직접.)

## 절차

1. 기간 결정: "주간"=지난 월~일 또는 최근 7일, "월간"=해당 월 1일~말일. 사용자가 기간을 말하면 그대로. 회신에 기간을 절대 날짜로 명시.
2. `sales-range` 로 원시 데이터 조회. **0건이면 "매출 없음"으로 단정하기 전에 기간·DB를 재확인**하고, 진짜 0건이면 그렇게 보고.
3. 집계 — **총판매와 반품을 분리한 뒤 순액 제시** (음수 행이 반품):
   - 총매출(양수 행 합) / 반품·환불액(음수 행 합) / **순매출** / 순마진 / 마진율(%)
   - 채널별 (오프라인 vs 자사몰) 순매출·비중
   - 베스트셀러 TOP 3~5 (순수량 기준, 동률이면 매출액)
   - 결제수단 분포 (간단히)
   - **부가세 예상액**: 순매출 × 10/110 (일반과세 가정 — 공급가액 = 순매출×100/110). 참고치임을 명시, 실제 신고는 세무 기준
   - 미지급 사입금: 재고이동 DB 지급상태=미지급 합계 (거래처별)
   - **브랜드별 순매출·비중** + **드롭 sell-through**(스타일별 판매수량 ÷ 입고수량 — 소진율. 드롭 후 2주 내 50% 미만이면 경고)
   - **담당별 처리 건수** (매출 담당 필드 기준 — 실적 평가용이 아니라 업무 분장 확인용임을 명시)
   - **정산 대사 알림**: 정산 DB에 대사상태=차이 건이 있으면 리포트 상단에 표시
   - **발주 알림**: PO 상태 발주확정/부분입고 건의 입고예정일 경과 여부
4. 재고 경고: `find-product` 대신 상품 DB 전체 조회가 필요하면 sales-range 데이터의 상품별 판매속도 기준으로 판단하고, 현재고 확인이 필요한 상품만 `find-product` 로 개별 조회. 품절 임박 판정은 상품 DB `재고상태` formula 가 SKU별 `재발주점`(비우면 1) 기준으로 자동 계산한다 — 직접 현재고와 비교하지 말 것.
5. Discord 회신 형식: 숫자는 원 단위 그대로(천 단위 콤마), 표는 짧게, 핵심 관찰 1~2문장 (예: "자사몰 비중이 지난주 20%→35%로 상승"). 이전 기간과 비교하려면 그 기간도 sales-range 로 실측 — 기억으로 비교하지 않는다.

## 자동 브리핑 (cron)

`scripts/shop_brief.py` 가 매일 push 용 텍스트를 생성한다 (조회 전용, LLM 불필요):

- `brief` — 아침: 오늘 일정 · 발주 진행(미입고 잔액+지연 D+N) · 재고 경고(품절임박/품절/음수) · 거래처 미지급 · 정산 차이 · 어제 매출
- `closeout` — 저녁: 오늘 매출 · 음수재고 · 내일 일정 · 미기록 확인 질문

cron 에 물려서 출력(stdout)을 쓰는 알림 채널(Discord/Slack 봇, 메일 등)로 보내면 된다:

```
47 8  * * *  python3 ~/.claude/skills/shop-report/scripts/shop_brief.py brief    | your-notifier
37 21 * * *  python3 ~/.claude/skills/shop-report/scripts/shop_brief.py closeout | your-notifier
```

## 주의

- 집계 숫자는 반드시 조회 데이터에서 계산한 값만. 관측하지 않은 수치를 보고하지 않는다.
- 마진율 = 총마진 / 총매출 × 100. 실판매가가 없는 행(마진 null)은 집계에서 제외하고 그 사실을 명시.
